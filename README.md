# Client
```
npx create-next-app@latest client
npx shadcn@latest init
npx shadcn@latest add card button input label tabs select badge separator alert
npm install react-hook-form
npm install zod
npm install @hookform/resolvers
npm install axios
```
